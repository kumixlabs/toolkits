---
"@kumix/storage": patch
---

Mark `@smithy/fetch-http-handler` as an optional peer dependency, matching the dynamic import with graceful fallback in the S3 SDK transport. Document it in the README install instructions.
