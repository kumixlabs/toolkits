---
"@kumix/utils": minor
---

Replace `clsx` and `tailwind-merge` with `cn` package and move external libraries (`@paralleldrive/cuid2`, `@sindresorhus/slugify`, `chrono-node`, `cn`, `consola`, `ms`, `nanoid`) to `peerDependencies`
